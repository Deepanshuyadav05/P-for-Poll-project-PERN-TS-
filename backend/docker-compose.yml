services:
  postgresql:
    image: postgres:17

    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: poll_app_DB

    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: